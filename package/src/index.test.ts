import { describe, it, expect } from "vitest"

describe('hello', () => {
  it('should pass', () => {
    expect(1).toBe(1)
  })
})

describe('hello', () => {
  it('should pass', () => {
    expect(2).toBe(2)
  })
})
